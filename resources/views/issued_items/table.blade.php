<div class="table-responsive">
    <table class="table table-striped border-bottom-0" id="issuedItemsTable">
        <thead>
        <tr class="fw-bold fs-6 text-muted">
            <th>{{ __('messages.issued_item.item') }}</th>
            <th>{{ __('messages.issued_item.item_category') }}</th>
            <th>{{ __('messages.issued_item.issued_date') }}</th>
            <th>{{ __('messages.issued_item.return_date') }}</th>
            <th>{{ __('messages.issued_item.quantity') }}</th>
            <th>{{ __('messages.common.status') }}</th>
            <th>{{ __('messages.common.action') }}</th>
        </tr>
        </thead>
        <tbody class="text-gray-600 fw-bold">
        </tbody>
    </table>
</div>
